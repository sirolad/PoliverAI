pydantic-settings>=2.3
python-dotenv==1.0.1
# Align fastapi with pyproject and avoid older incompatible pin
fastapi>=0.115,<1.0
uvicorn==0.30.1
pymongo>=4.0
python-multipart>=0.0.7
requests==2.32.3
argon2-cffi>=21.3.0
python-jose[cryptography]>=3.3.0
PyYAML>=6.0
chromadb==0.5.3
streamlit==1.37.1

# LLM & NLP
openai==1.40.0

# Optional: sentence-transformers is required for some embedding/semantic search features.
# Note: this may pull in heavy dependencies (torch). If you want a CPU-only install, consider
# pinning a cpu build of torch or installing sentence-transformers inside a separate dev venv.
# dev should turn comment it out for speed build speed
# transformers==4.44.0
# sentence-transformers==3.0.1
# ###

tiktoken==0.7.0


# Vector DB
faiss-cpu==1.8.0
pinecone-client==3.2.2 # optional, if using Pinecone
weaviate-client==4.6.4 # optional, if using Weaviate


# Data & Utils
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.1
requests==2.32.3
beautifulsoup4==4.12.3 # for scraping GDPR text
python-docx>=1.1 # provides `docx` module used by ingestion

pdfplumber>=0.11

# CA bundle helper for TLS (used to avoid SSL handshake failures when connecting to Atlas)
certifi>=2024.9.0

# protobuf constraint to satisfy google-cloud-aiplatform
# google-cloud-aiplatform requires protobuf>=3.19.5 and <5.0.0dev
protobuf

# Google Cloud Storage client (optional, needed for GCS report uploads)
google-cloud-storage==2.11.0

# Ensure python-docx is at least a modern release (0.8.11 is available on PyPI)
python-docx

# PDF Reports
reportlab==4.2.0


# Testing
pytest==8.2.2
httpx==0.27.0
stripe==5.0.0
# rag==0.1.0
# domain-models==0.1.0
gradio==4.35.0
email_validator
pymongo>=4.0
pyflakes