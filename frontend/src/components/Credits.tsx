export default function Credits() {
  return (
    <div className="p-4">
      <h2 className="text-lg font-semibold">Credits</h2>
      <p className="text-sm text-gray-600">Transaction history UI (placeholder). I replaced the previous corrupted file with a minimal component to unblock the build. I'll reintroduce the detailed UI and fixes in small, tested patches next.</p>
    </div>
  )
}
