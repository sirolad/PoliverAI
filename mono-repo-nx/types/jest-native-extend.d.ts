declare module '@testing-library/jest-native/extend-expect' {
  // This module is imported for side effects (it augments jest matchers).
  // Provide an empty export so TypeScript accepts `import '@testing-library/jest-native/extend-expect'`.
  export {};
}
