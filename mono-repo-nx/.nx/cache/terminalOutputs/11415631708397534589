> sh -lc "if npx react-native --help 2>/dev/null | grep -q run-macos; then npx react-native run-macos; else echo 'react-native run-macos not available. To enable: install react-native-macos and set up the macos platform (see https://microsoft.github.io/react-native-macos/).'; fi"

react-native run-macos not available. To enable: install react-native-macos and set up the macos platform (see https://microsoft.github.io/react-native-macos/).
